default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: rfe_ready_development

test:
  <<: *default
  database: rfe_ready_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
